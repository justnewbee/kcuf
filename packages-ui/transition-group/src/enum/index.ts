export enum ETransactionStatus {
  ENTERING = 'entering',
  ENTERED = 'entered',
  EXITING = 'exiting',
  EXITED = 'exited',
  UNMOUNTED = 'unmounted'
}

export enum ETransactionMode {
  OUT = 'out-in',
  IN = 'in-out'
}
