export { default } from './with-model';

export {
  DialogMode,
  DialogSize,
  useDialog
} from '@kcuf-ui/rc-dialog-headless';

export type {
  ModelProps as DialogProps,
  DialogButtonProps,
  DialogContext
} from '@kcuf-ui/rc-dialog-headless';
