export { default } from './with-model';

export {
  DialogMode,
  DialogSize,
  useDialog
} from '@kcuf-ui/dialog-headless';

export type {
  ModelProps as DialogProps,
  DialogButtonProps,
  DialogContext
} from '@kcuf-ui/dialog-headless';
