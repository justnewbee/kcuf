export { default } from './with-model';

export {
  DialogMode,
  DialogSize,
  useDialog
} from '@kcuf-ui/rc-dialog-headless';

export type {
  DialogProps,
  DialogButtonProps,
  DialogContext
} from '@kcuf-ui/rc-dialog-headless';
