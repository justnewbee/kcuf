// import {
//   hslToRgb,
//   numberToHex,
//   hexShorthand
// } from '../util';

// function colorToHex(color: number): string {
//   return numberToHex(Math.round(color * 255));
// }

// function convertToHex(red: number, green: number, blue: number): string {
//   return hexShorthand(`#${colorToHex(red)}${colorToHex(green)}${colorToHex(blue)}`);
// }

export default function convertToHexString(input: string): string {
  return input; // TODO
}
