/**
 * @vitest-environment jsdom
 */
import {
  describe,
  expect,
  test,
  beforeEach
} from 'vitest';

import lib from '../src';

describe('xx', () => {
  beforeEach(() => {
    // TODO
  });
  
  test('xx', () => {
    expect(lib).toBeTypeOf('function');
  });
});
